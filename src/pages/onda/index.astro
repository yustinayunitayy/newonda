---
import Layout from '../../layouts/Layout.astro'
import Navbar from '../../components/global/Navbar.astro'
import BlockRenderer from '../../components/section/BlockRenderer.astro'
import { getPage } from '../../lib/data/pages'
import { fetchSiteSettings } from '../../lib/data/site-settings'

const [page, settings] = await Promise.all([getPage('onda'), fetchSiteSettings()])
const blocks = page?.blocks ?? []
---

<Layout
  title={page?.meta?.title}
  description={page?.meta?.description}
  ogImage={page?.meta?.image?.url}
>
  <Navbar variant="transparent" scrollBehavior="scroll-to-white" />
  <h1 class="sr-only">ONDA & ONDA Valve — Kran, Shower, Valve & Plumbing Indonesia</h1>
  <div class="relative pb-20 md:pb-28">
    <BlockRenderer blocks={blocks} settings={settings ?? undefined} />
  </div>
</Layout>
